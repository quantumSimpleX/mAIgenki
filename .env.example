# Tier-0-only: the shared anonymous free-tier key baked into the app build.
# NOT for user-connected provider keys/tokens -- those live in the on-device
# KeyStore (SecureStore/localStorage via src/lib/llm/keystore.ts), never here
# and never in SQLite.
EXPO_PUBLIC_MAIGENKI_OPENROUTER_API_KEY=
