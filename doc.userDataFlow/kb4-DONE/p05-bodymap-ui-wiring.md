# P5: Bodymap UI Wiring

## Scope
Tasks 5.1–5.7 in `doc.userDataFlow/userDataTask.md`, Phase 5 — multi-location dot rendering, persistent image/chart timeline, real image thumbnails/lightbox.

## Out of Scope
Editing non-primary condition locations (future work per PRD §11).

## Dependencies
Phase 2 in `kb4-DONE` (needs `getConditionLocations`/`getRecordImageThumbnail`/`getRecordImageBlob` and the app cutover from Task 2.13). Phase 4 in `kb4-DONE` for real images to display.

## Assigned Agents
- ArchAgent:
- DevAgent: DevAgent (session interrupted mid-verification by a spend-limit cutoff — orchestrator resumed and completed verification/documentation; source implementation itself was already complete when the session ended)
- QAAgent:

**Note (2026-07-31, orchestrator):** The devAgent session implementing this card was killed mid-run by an API spend-limit error while running its final test/lint checks — the source changes were already complete on disk, but the card file itself was never updated (Implementation Checklist/Record were still the empty template). The orchestrator independently verified the implementation (typecheck, lint, `__tests__/screens/bodymap.test.tsx`, `__tests__/lib/demoParity.test.ts`, full `npm test`), found and fixed one real gap (see Implementation Record), and completed this card's documentation before moving it to `kb3-TEST`. Manual browser verification (Task 5.7) remains genuinely undone — see Blockers.

## Allowed Files/Directories
`src/model/conditions.ts`, `src/hooks/useConditions.ts`, `src/app/bodymap.tsx`, this card.

## Implementation Checklist

- [x] P05-01 **Task 5.1 — Extend `ConditionRecord` type** — `src/model/conditions.ts` gained `imageId?: string | null`, `mimeType?: string | null` (lazy references only, per the PRD's no-raw-bytes-inline rule).
- [x] P05-02 **Task 5.2 — `useConditionDots` hook** — `src/hooks/useConditions.ts`: new `useConditionDots(sourceOverride?)`, mirrors `useConditions()`'s fallback-seeded-state + `useOptionalIndexedDb()` + effect pattern; returns `[dots, refresh]` so callers can re-fetch after relocation/upload.
- [x] P05-03 **Task 5.3 — Refactor dot-rendering components** — `GhostDots`/`BodySvg`/`ConditionRipples` in `src/app/bodymap.tsx` now consume the flattened `IndexedConditionDot[]` from `useConditionDots()` (wired at line ~1984) instead of reading `cx_percent`/`cy_percent` off `DesignCondition[]` directly. `pressNearest` still resolves a tapped dot's `conditionId` back to the full `DesignCondition` via the separate `useConditions()`-sourced array (two parallel lists joined by id, not merged), per the task's explicit instruction.
- [x] P05-04 **Task 5.4 — Make the image/chart timeline persistent** — `RecordsCarousel` (line 1253) now renders on `selectedCondition && condRecords.length > 0`, no longer gated on `chatOpen`.
- [x] P05-05 **Task 5.5 — Real image thumbnails** — `renderRecordThumb` (line 271) branches on `rec.imageId`; new `RecordThumbImage` component lazy-fetches `getRecordImageThumbnail` in a `useEffect` keyed on `imageId`, converts to an object URL (revoked on cleanup), falls back to the existing SVG placeholder while loading/on error.
- [x] P05-06 **Task 5.6 — Real images in the lightbox** — `RecordLightbox` (line 1438) gets the same lazy-fetch pattern via new `LightboxFullImage`, calling `getRecordImageBlob` (full resolution) only when the lightbox record changes and has an `imageId`.
- [ ] P05-07 **Task 5.7 — Manual UI regression pass** — **NOT DONE.** Neither the interrupted devAgent session nor the orchestrator ran a real browser (no live `npx expo start --web` + visual check performed). See Blockers.

## Acceptance Criteria
A multi-location condition renders multiple dots, all opening the same condition sheet. The image timeline is visible whenever a condition is selected, not gated on chat. Demo data (post-cutover) renders visually identical to the pre-cutover SQLite-backed demo.

## Required Validation
- Manual: demo flow regression pass across desktop and mobile viewport widths.
- `npm run typecheck`, `npx expo lint`

## Developer Verification

- 2026-07-31 (orchestrator, post-crash resumption):
  - `npm run typecheck` — clean on `src/app/bodymap.tsx`, `src/hooks/useConditions.ts`, `src/model/conditions.ts`. Remaining errors confined to the same 4 pre-existing files documented in every prior phase card (`tests/lib/_spike01-pdfjs-canvas-blob.test.ts`, `tests/lib/_spike03-pdf-text-timing.test.ts`, `tests/lib/backup.test.ts`, `tests/lib/lmf/oauthPkce.test.ts`).
  - `npx expo lint` — 2 errors, 10 warnings total, all pre-existing (confirmed via `git diff` showing zero changes at the flagged lines): `bodymap.tsx:1685` (`setState` synchronously in effect, unrelated `settingsOpen` effect), `bodymap.tsx:12` (unused `G` import), `ProviderSettings.tsx:182` (same lint rule, unrelated file). None of the 12 problems are on lines this card's diff touches.
  - `npx jest __tests__/screens/bodymap.test.tsx __tests__/lib/demoParity.test.ts --runInBand --silent` — **2 suites, 19/19 passed.**
  - `npm test` (full suite) — **61 suites, 562 tests: 559 passed, 3 failed** — the same 3 pre-existing failures documented in `kb4-DONE/p04-image-capture-pipeline.md` (2 in `pipeline-process.test.ts`, 1 in `provider-recovery.test.ts`, SQLite-path-only). No new failures.

## QA Test Plan

Independent verification approach (equivalence partitioning on the two claims most load-bearing for this card: the defect-fix claim, and the Task 5.3 dot→condition join), plus a direct-code re-derivation of every other claim rather than trusting the Implementation Record's prose.

1. **Defect-fix claim** — read `ConditionRecordEntry` in `src/lib/db/indexedDb.ts` directly; read `toConditionRecord()` in `src/hooks/useConditions.ts` directly; trace `rec.imageId` consumption in `renderRecordThumb`/`RecordLightbox` in `src/app/bodymap.tsx`; trace the demo seed (`seedIndexedDbDemoData`) to confirm a real `image_id` is actually seeded on `r-stone-1` so the fix is exercised on the demo path, not just theoretically reachable.
2. **Task 5.3 join requirement** — read `GhostDots`'s `pressNearest` in `bodymap.tsx` line-by-line to confirm the tapped dot's `conditionId` is resolved against the separate `useConditions()`-sourced array via `.find()`, not a merged/single list.
3. **Test/typecheck/lint counts** — re-run all four Required Validation + Developer Verification commands from a clean shell and diff actual output against the card's claimed numbers, including confirming (via `git diff --stat`/hunk headers) that the flagged pre-existing lint/typecheck lines fall genuinely outside this card's diff rather than trusting the "confirmed via git diff" claim at face value.
4. **Blocker #1 (Task 5.7 manual pass)** — attempt real browser verification via `npx expo start --web`, since a subagent with Playwright tooling was available this session (not assumed available for future QA passes). Targeted checks: bilateral `stones` dots open the same sheet, records carousel shows without chat open, `r-stone-1`'s thumbnail/lightbox render via a real `<img>`/`expo-image` element rather than the SVG placeholder path, no console errors during the flow.

## Implementation Record

- 2026-07-31, DevAgent (session interrupted before writing this section): implemented Tasks 5.1–5.6 per the plan above. Diff stats: `src/app/bodymap.tsx` +164/-59 (net +223 lines touched), `src/hooks/useConditions.ts` +67 lines (new `useConditionDots`, new `toConditionRecord` mapper, cutover of `useConditions`/`useConditionRecords` from `useOptionalDatabase`/`queries.ts` to `useOptionalIndexedDb`/`indexedDb.ts`), `src/model/conditions.ts` +4 lines (`imageId`/`mimeType` fields). The session was killed by a spend-limit API error while running its final "run the relevant test suites" step — the last message captured was confirmation that only pre-existing lint warnings remained.
- 2026-07-31, orchestrator (resumption): independently re-derived and verified the above via direct source reads and the Developer Verification commands. **Found and fixed one real defect**, not caught by the interrupted session: `toConditionRecord()` in `useConditions.ts` (which maps the raw `ConditionRecordEntry` IndexedDB row onto the `ConditionRecord` shape Tasks 5.5/5.6 consume) never carried `image_id` through — `ConditionRecord.imageId` was always `undefined` regardless of what was actually stored, which would have silently made Tasks 5.5/5.6's `rec.imageId` branches in `bodymap.tsx` dead code (always falling back to SVG placeholder art, even for the Phase 2 demo condition that has a real placeholder image linked via `condition_records.image_id`). Fix: added `imageId: r.image_id` to the object literal in `toConditionRecord` (`src/hooks/useConditions.ts`). One-line, mechanical, verified against `ConditionRecordEntry`'s actual `image_id: string | null` field (`src/lib/db/indexedDb.ts`). `ConditionRecord.mimeType` was left unmapped here deliberately — it's not read anywhere in `bodymap.tsx`'s Task 5.5/5.6 code paths; `getRecordImageBlob` (used by `LightboxFullImage`) returns `mimeType` directly from its own read, so the type field is currently unused/reserved rather than a second gap of the same kind.
  - Re-ran typecheck and the two directly-relevant test suites after the fix — both clean (see Developer Verification). **No dedicated regression test exists yet asserting `useConditionRecords`'s output carries `imageId` through from a seeded `condition_records.image_id` row** — this fix is verified by code inspection and by the fact that `renderRecordThumb`/`RecordLightbox`'s existing branches now have a non-`undefined` value to branch on for the demo's image-linked condition, but not by a new assertion. Recommend QA either write one or explicitly accept this as covered indirectly by Task 5.7's eventual manual pass.

## QA Record

- 2026-07-31, QAAgent: Independently verified this card. **Verdict: PASS.**
  - **Defect-fix claim (orchestrator's `toConditionRecord` fix) — CONFIRMED REAL AND CORRECT.** Read `ConditionRecordEntry` in `src/lib/db/indexedDb.ts:79-92` directly: `image_id: string | null` is a genuine field on the raw IndexedDB row. Read `toConditionRecord()` in `src/hooks/useConditions.ts:23-32` directly: it now includes `imageId: r.image_id` in the returned object literal — the fix is present and mechanically correct (straight field passthrough, no transform needed). Confirmed this was genuinely load-bearing, not theoretical: traced `rec.imageId` consumption in `bodymap.tsx` — `renderRecordThumb` (line 273) and `RecordLightbox` (line 1456) both branch on `rec.imageId` truthiness to choose the real-image path (`RecordThumbImage`/`LightboxFullImage`, lazy `getRecordImageThumbnail`/`getRecordImageBlob` fetches) vs. the SVG placeholder path; without the fix `imageId` would always be `undefined` and these branches would be dead code. Also confirmed the demo path actually exercises this: `seedIndexedDbDemoData` (`indexedDb.ts:496-510`) sets `image_id: r.id === 'r-stone-1' ? DEMO_IMAGE_ID : null` when seeding `condition_records`, so `r-stone-1` (KUB X-ray, under the `stones` condition) is a real non-null case in demo data, not a hypothetical.
  - **Task 5.3 dot→condition join — CONFIRMED CORRECT.** Read `GhostDots`'s `pressNearest` (`bodymap.tsx:599-610`) directly: it finds the nearest dot in the flattened `visible: IndexedConditionDot[]` list by distance, then resolves `nearest.conditionId` against the separate `conditions: DesignCondition[]` prop (the `useConditions()`-sourced array) via `conditions.find((c) => c.id === nearest.conditionId)`. This is genuinely two parallel lists joined by id at press time, not a merge — matches the task's explicit requirement.
  - **Test/typecheck/lint counts — ALL CONFIRMED, no discrepancies found.**
    - `npx jest __tests__/screens/bodymap.test.tsx __tests__/lib/demoParity.test.ts --runInBand --silent` → **2 suites, 19/19 passed.** Matches claim exactly.
    - `npm run typecheck` → confirmed zero errors on `src/app/bodymap.tsx`, `src/hooks/useConditions.ts`, `src/model/conditions.ts`; all errors confined to the same 4 pre-existing files (`tests/lib/_spike01-pdfjs-canvas-blob.test.ts`, `tests/lib/_spike03-pdf-text-timing.test.ts`, `tests/lib/backup.test.ts`, `tests/lib/lmf/oauthPkce.test.ts`) — verified by grepping the full typecheck output for file paths, not just spot-checking. Matches claim.
    - `npx expo lint` → **2 errors, 10 warnings, 12 total.** Matches claim exactly. Independently re-verified (not just trusted) that both flagged errors (`bodymap.tsx:1685`, `bodymap.tsx:12`) fall outside this card's diff: ran `git diff HEAD -- src/app/bodymap.tsx | grep '^@@'` and confirmed no hunk touches lines 12 or 1685 (nearest hunks are at line ranges 19-21 and 1085+; line 1685's `useEffect(() => { if (!settingsOpen) setOpenDropdown(null) }, [settingsOpen])` and the unused `G` import at line 12 are both genuinely untouched pre-existing code).
    - `npm test` (full suite) → **61 suites, 562 tests: 559 passed, 3 failed.** Matches claim exactly. Failures are the same `pipeline-process.test.ts` (2) / `provider-recovery.test.ts` (1) pre-existing SQLite-path failures documented and stash-verified in `kb4-DONE/p04-image-capture-pipeline.md` and `p03-extraction-pipeline-rebuild.md` — same assertion mismatches, byte-identical failure text, unrelated to this card's diff.
  - **Real browser verification (Blocker #1) — PERFORMED, not skipped.** Started `npx expo start --web --port 8099` in this session and used a Playwright-capable subagent (uiux-auditor) to drive a real Chrome instance against it. Results, all PASS:
    - Body map renders correctly (SVG/PNG layers, legend, timeline), zero console errors across the full flow (only 4 pre-existing warnings — require cycles and a `pointerEvents` deprecation — present even before any interaction, unrelated to this card).
    - The bilateral `stones` condition renders two dots (left/right kidney, confirmed via `indexedDb.ts:518-522` seed positions at `cx_percent ∓ 4`); clicking either dot opened an identical sheet (same title 尿路結石/Kidney stones, same medical name, same note, same source) — confirms both Task 5.3's dot flattening and the id-join in `pressNearest` work end-to-end on live demo data, not just in Jest.
    - The records carousel (KUB X-ray, Stone analysis thumbnails) is visible directly under the condition header with the chat panel closed — confirms Task 5.4's persistent-regardless-of-chat-state requirement live.
    - The KUB X-ray thumbnail and lightbox (the `r-stone-1` record) both render through an `expo-image`/`<img>` DOM element (confirmed via DOM inspection, `data-expoimage="true"` wrapper) rather than inline SVG placeholder shapes — the "Stone analysis" record (no `imageId`) by contrast renders as SVG bar shapes, giving a direct visual contrast that proves the `imageId`-branch is live and the orchestrator's fix is functioning end-to-end, not just type-correct. The image itself renders as a blank/dark rectangle (expected — the demo's placeholder blob is a 4×1px near-empty PNG, not real X-ray content — this is a demo-data limitation, not a bug).
    - Dev server stopped after verification; QA screenshot artifacts removed from repo root (not committed).
  - This QA pass materially strengthens the card beyond what Jest alone could confirm: Blob→object-URL→`<img>` rendering (explicitly untestable in Jest, per Blocker #2) was confirmed working live, and the multi-dot-to-same-sheet flow was confirmed against real coordinate-based click delegation (not just prop-level unit assertions).
  - **No new defects found.** All claims in the Implementation Record and Developer Verification checked out under independent re-derivation.
  - **Recommendation on Blocker #1 / Task 5.7:** Given that a full manual regression pass (including mobile-viewport widths, which this session's spot-check did not cover) is now *partially* satisfied by this session's live browser verification, downgrade Blocker #1 from "genuinely undone" to "narrowed but not fully closed" — the specific Task 5.3/5.4/5.5/5.6 mechanisms this card introduces are now proven live on desktop viewport; a full cross-viewport/mobile pass per Task 5.7's original scope is still outstanding. Recommend accepting this as sufficient to move to `kb4-DONE`, consistent with how Phase 4's Canvas-rendering gap was handled, with the residual mobile-viewport pass noted as a carried-forward known gap rather than a blocker — the core functional risk (dead-code branches from the missing `imageId` mapping) that made this gap concerning is now the specific thing that was live-verified.

## Defects and Retests

- (Resolved before QA) `useConditions.ts`'s `toConditionRecord` didn't map `image_id` → `imageId`, defeating Tasks 5.5/5.6. Fixed by the orchestrator during post-crash resumption — see Implementation Record.

## Blockers

1. **Task 5.7 (manual UI regression pass) is genuinely undone.** No real browser was launched by either the interrupted devAgent or the orchestrator resuming this card. Per this project's `CLAUDE.md` ("For UI or frontend changes, start the dev server and use the feature in a browser before reporting the task as complete... if you can't test the UI, say so explicitly rather than claiming success"), this must not be treated as verified. Everything else on this card is verified via typecheck/lint/Jest only. **Recommend**: before this card is treated as fully production-complete, run `npx expo start --web`, load the demo body map, and confirm — multi-location dots (the seeded bilateral `stones` condition) render two dots opening the same sheet; the records carousel shows regardless of chat state; the one demo condition with a real placeholder image (`r-stone-1`, per Phase 2's `seedIndexedDbDemoData`) shows a real thumbnail/lightbox image instead of SVG placeholder art (this is also the first real end-to-end proof the `toConditionRecord` fix above actually works, since Jest can't render a live `<img>`/object-URL flow meaningfully). This can be QA-approved with this gap flagged (matching how Phase 4's Canvas-rendering gap was handled) rather than blocking indefinitely, at the orchestrator's/user's discretion.
2. Real Blob rendering through `URL.createObjectURL` in `RecordThumbImage`/`LightboxFullImage` inherits the same category of Jest-environment limitation already documented in `kb4-DONE/p02-schema-Phase-persistence-layer.md` (Blocker #2) and `p04-image-capture-pipeline.md` — not testable end-to-end outside a real browser.

## Completion

- Implementation (Tasks 5.1–5.6) complete and verified via typecheck/lint/Jest by the orchestrator after the implementing devAgent's session was cut off by a spend-limit error. One real defect found and fixed during verification (see Implementation Record). Task 5.7 remains an open, honestly-documented manual-verification gap. Moving to `kb3-TEST` for independent QA, which should evaluate whether the Task 5.7 gap should block `kb4-DONE` or be accepted as a documented known limitation (consistent with how Phase 4's Canvas-rendering gap was handled).
- 2026-07-31, QAAgent: **QA PASS.** All Developer Verification claims and the orchestrator's defect-fix independently re-derived and confirmed correct (see QA Record). Additionally performed a live browser pass (desktop viewport only) via a Playwright-capable subagent against a locally started dev server, confirming the multi-dot join, persistent records carousel, and real-image thumbnail/lightbox rendering all work end-to-end on live demo data — narrowing Blocker #1 from "fully undone" to "desktop-verified, mobile-viewport pass still outstanding." Recommend `kb3-TEST` → `kb4-DONE` with the residual mobile-viewport regression pass carried forward as a documented known gap.

## History

- 2026-07-31: Card moved `kb1-TODO` → `kb2-CODE`. DevAgent implemented Tasks 5.1–5.6; session was interrupted by an API spend-limit error before the card was documented or moved.
- 2026-07-31: Orchestrator resumed the card — verified the implementation independently, found and fixed one real defect (`toConditionRecord` missing `imageId` mapping), completed the card's documentation, and moved `kb2-CODE` → `kb3-TEST` for QA.
