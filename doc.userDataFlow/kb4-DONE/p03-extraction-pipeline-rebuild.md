# P3: Extraction Pipeline Rebuild

## Scope
Tasks 3.1–3.7 in `doc.userDataFlow/userDataTask.md`, Phase 3 — page boundaries, structure analysis, chunking, bounded concurrency, `enrich.ts` rewrite, and wiring chunk output into `pipeline.ts` via IndexedDB.

## Out of Scope
Image capture (Phase 4), UI wiring (Phase 5).

## Dependencies
Phase 2 must be in `kb4-DONE` (Task 3.6 needs `putConditionLocation`/computed-position return from Tasks 2.4/2.7, and Task 2.13's app cutover so `pipeline.ts` actually receives an `IDBDatabase`). Task 3.1 depends on Task 0.3's benchmark result.

## Assigned Agents
- ArchAgent:
- DevAgent: DevAgent (this session)
- QAAgent:

## Allowed Files/Directories
`src/lib/pdf/extract.ts`, `src/lib/llm/structure.ts`, `src/lib/llm/chunk.ts`, `src/lib/llm/pool.ts`, `src/lib/llm/enrich.ts`, `src/lib/pipeline.ts`, corresponding test files, this card.

**Scope-expansion note (2026-07-31, DevAgent):** Two files outside this list needed a minimal, additive edit to actually complete Task 3.6's wiring — see Implementation Record for why each was unavoidable within the shared-persistence-path architecture Phase 2 established:
- `src/lib/db/indexedDb.ts` — `persistEnrichmentResult` hardcoded `inferred_fields: null`; Task 3.6 requires it to persist the new `inferred_from_structure` field, which only this function can do without duplicating/bypassing Phase 2's single write path.
- `tests/lib/pipeline.test.ts` — rewritten to mock `persistEnrichmentResult`/`idb` instead of the retired `queries.ts` insert-call mocks, matching Task 3.6's `pipeline.ts` wiring (corrected 2026-07-31 per QA finding — this file is in-scope as pipeline.ts's "corresponding test file"; the change is a full mock-target rewrite, not a 2-line tidy-up).

## Implementation Checklist

- [x] P03-01 **Task 3.1 — Page boundaries in text extraction** — `ExtractionResult.pageBreaks` added to `src/lib/pdf/extract.ts`. Web path: real per-page offsets from the existing per-page loop (free — no added latency, per Task 0.3's benchmark caveat only applying to the cost of computing offsets, not to bookkeeping that already happens). Native path (`expo-pdf-text-extract`, one text blob, no per-page structure exposed): length-proportional estimate (`estimatePageBreaks`), matching Task 0.3's recommended fallback.
- [x] P03-02 **Task 3.2 — Structure-analysis module** — new `src/lib/llm/structure.ts`: `analyzeRecordStructure()`, one `callLLMWithFallback` call (`STRUCTURE_PROMPT`, mirrors `enrich.ts`'s existing call pattern), synthetic single-section fallback on total LLM failure (never throws). `EnrichRoutingOptions` (the shared LLM-routing bag) also lives here — see Implementation Record for why.
- [x] P03-03 **Task 3.3 — Chunking module** — new `src/lib/llm/chunk.ts`: `chunkRecordBySections()`, pure (no LLM calls), re-anchors every section via `text.indexOf(heading, cursor)` (never trusts the LLM's reported offsets), splits oversized sections on paragraph boundaries (hard-splitting any single paragraph still too large), merges sections under 200 chars into a neighbor.
- [x] P03-04 **Task 3.4 — Bounded concurrency pool** — new `src/lib/llm/pool.ts`: `runWithConcurrency()`, dependency-free fixed-size worker pool pulling from a shared cursor.
- [x] P03-05 **Task 3.5 — Rewrite `enrich.ts` orchestration** — replaced the old `INVENTORY_PROMPT`/`CONDITION_PROMPT` inventory→per-condition loop with `CHUNK_EXTRACTION_PROMPT` (one call per `TextChunk`). Extended `ConditionInput`/`MeasurementInput` with `inferred_from_structure?: string[]`; `ConditionInput.locations?` already existed from Phase 2 (verified, no change needed). Added `EnrichmentResult.partialFailures?`. New `enrichFromText`: structure → chunk → bounded pool → cross-chunk merge (dedup by normalized name+organ+location; earliest date wins; evidence/care_events/locations concatenate) → partial-failure-tolerant return, `EnrichmentFailedError` only when every chunk failed. `onChunkProgress?(completed, total)` matches the old `onConditionProgress` call shape exactly.
- [x] P03-06 **Task 3.6 — Wire chunk output into `pipeline.ts` via IndexedDB** — `pipeline.ts` now captures `pageBreaks` from the PDF extraction step and threads it through `EnrichRoutingOptions.pageBreaks` into `enrichFromText`/`analyzeRecordStructure`. `enrichment.partialFailures` is traced via the existing `trace()` helper. The `is_primary` `condition_locations` write + `c.locations` loop were already implemented by Phase 2's `persistEnrichmentResult` (`indexedDb.ts`); the only missing wiring was `inferred_fields` persistence, added there (see Implementation Record).
- [x] P03-07 **Task 3.7 — Unit tests for Phase 3** — `tests/lib/pool.test.ts`, `tests/lib/chunk.test.ts` (new — see Implementation Record for why not `src/lib/llm/*.test.ts` as the task text literally says), `tests/lib/enrich.test.ts` extended with structure-analysis-failure fallback, one-chunk-failure partial results, all-chunks-failing throw, and cross-chunk dedup (earliest date + merged evidence) tests.

## Acceptance Criteria
Every checklist item is implemented and independently QA-approved. LLM attempt count scales with chunk count, not condition count. A record with one failing chunk still produces a usable partial result.

## Required Validation
- `npx jest tests/lib/pool.test.ts tests/lib/chunk.test.ts tests/lib/enrich.test.ts --runInBand --silent` — **PASS**, 3 suites, 32/32 tests. (Deviates from the card's literal `src/lib/llm/pool.test.ts src/lib/llm/chunk.test.ts` paths — see Implementation Record; `jest.config.js`'s `testMatch` only picks up `__tests__/**` and `tests/**`, so a `src/lib/llm/*.test.ts` file is silently never run.)
- `npm run typecheck` — **PASS** for every file this card touches (including the 2 expanded-scope files). Errors remain only in the 4 files already documented as pre-existing/unrelated in `kb4-DONE/p02-schema-Phase-persistence-layer.md`: `tests/lib/_spike01-pdfjs-canvas-blob.test.ts`, `tests/lib/_spike03-pdf-text-timing.test.ts` (missing `@types/node`), `tests/lib/backup.test.ts` (implicit-any), `tests/lib/lmf/oauthPkce.test.ts` (missing `@types/node`).
- `npx jest tests/lib/pipeline.test.ts tests/lib/pdf.test.ts __tests__/lib/pdf-extract-web.test.ts __tests__/lib/pipeline-process.test.ts tests/lib/indexedDb.test.ts tests/lib/indexedDbBackup.test.ts --runInBand --silent` — **4 suites pass in full; `__tests__/lib/pipeline-process.test.ts` has 2 pre-existing failures** (`enrichFromText` called with extra profile/onTrace args the test's `toHaveBeenCalledWith` doesn't account for — same failure already documented in `kb4-DONE/p02-schema-Phase-persistence-layer.md`'s Blockers, reproduced with identical assertion-mismatch shape here; confirmed not a regression by isolating this suite and comparing the failure text verbatim).
- `npm test` (full suite, `jest --coverage`) — **60 suites, 549 tests: 546 passed, 3 failed.** All 3 failures pre-existing (2 in `pipeline-process.test.ts` above, 1 in `__tests__/db/provider-recovery.test.ts`, a fully SQLite-path test this card never touches — identical to the 3 documented in `kb4-DONE/p02-schema-Phase-persistence-layer.md`). Coverage: **87.29% stmts / 73.83% branch / 83.81% funcs / 89.28% lines** — above the 80%-lines global threshold.

## Developer Verification
All Required Validation commands above were actually run (not assumed) — exact pass/fail counts recorded, not "should pass."

## QA Test Plan

Independent verification of DevAgent's Tasks 3.1–3.7 claims. Techniques: code reading (full review of all 3 new small modules, not sampled), re-execution of every Required Validation command, and cross-referencing the card/Implementation Record against `userDataTask.md`'s Phase 3 prose for drift.

1. Cross-check each of the 7 checklist items (P03-01..07) against `userDataTask.md` Tasks 3.1–3.7's literal signatures/prose.
2. Full read of `src/lib/llm/structure.ts`, `chunk.ts`, `pool.ts` — confirm `chunk.ts` has zero LLM calls (pure), confirm every emitted `TextChunk.text` is provably an exact substring of the source (re-anchoring logic, not just the claim), confirm `pool.ts` never exceeds `limit` concurrent workers and that one rejection doesn't block/skip others.
3. Read `enrich.ts`'s rewritten orchestration — confirm structure-analysis failure never throws (resilience fallback), a chunk failure among several still returns partial results without throwing, all-chunks-failing throws `EnrichmentFailedError`, and `onChunkProgress`'s call shape matches the old `onConditionProgress` (checked against `pipeline.ts`'s progress-math call sites).
4. Re-run `npx jest tests/lib/pool.test.ts tests/lib/chunk.test.ts tests/lib/enrich.test.ts --runInBand --silent`, `npm run typecheck`, the broader adjacent-suite command, and full `npm test --coverage`; diff actual output against the card's claimed counts.
5. Verify each of the 4 documented deviations against the actual diff (not just the prose describing it), especially the `indexedDb.ts` and `tests/lib/pipeline.test.ts` "expanded scope" edits.
6. No accessibility/UI surface in this card's scope (pure backend/orchestration change) — not applicable, noted rather than skipped silently.

## Implementation Record

### 2026-07-31 — DevAgent, Tasks 3.1–3.7

**Files changed:**
- `src/lib/pdf/extract.ts` — `ExtractionResult.pageBreaks?: number[]` (optional at the type level, not just in practice, so pre-existing test fixtures across the repo that construct an `ExtractionResult` without it keep typechecking without every one needing an edit). `estimatePageBreaks()` (even split) for the native path; the web path's existing per-page loop records real offsets for free.
- `src/lib/llm/structure.ts` (new) — `SectionType`, `RecordSection`, `RecordStructure`, `EnrichRoutingOptions`, `analyzeRecordStructure()`, `STRUCTURE_PROMPT`. The LLM reports approximate `startOffset`/`endOffset` per section; this module resolves `pageStart`/`pageEnd` against `pageBreaks` but does **not** re-anchor the character offsets themselves — that's `chunk.ts`'s job (Task 3.3), by design, so the "never trust the LLM's offsets blindly" requirement lives in exactly one place.
- `src/lib/llm/chunk.ts` (new) — `TextChunk`, `chunkRecordBySections()`, `reanchorSections()` (heading-string search with a monotonically advancing cursor, so repeated headings resolve to successive occurrences; falls back to the LLM's offset clamped into range if the heading isn't found verbatim), `mergeTinySections()`, `splitOversized()` (paragraph-boundary split, offset-based via `text.slice` throughout — never string-rejoin — so every emitted chunk is a guaranteed exact substring of the source, verified directly by `tests/lib/chunk.test.ts`'s offset-accuracy suite), `hardSplitIfNeeded()`.
- `src/lib/llm/pool.ts` (new) — `runWithConcurrency()`: a shared `cursor` variable and a fixed array of `poolSize` async workers each pulling the next unclaimed item in a `while` loop; per-item try/catch maps a rejection to `{status:'rejected', reason}` at that item's own index without stopping other workers.
- `src/lib/llm/enrich.ts` — full orchestration rewrite (see checklist). `mergeConditions`/`mergeTwoConditions` key on `normalizedKey(name_medical)|normalizedKey(organ)|normalizedKey(anatomical_location)`; `mergeMeasurements` keeps the pre-existing name+value+unit+date dedup logic, now applied across all chunks' output rather than one inventory-loop's accumulator. `providers` is now derived once from the final merged conditions' own `provider` field (mirrors the old per-condition `if (condition.provider) providers.push(...)` behavior, but after merge instead of during the loop, since two chunks can now describe the same condition).
- `src/lib/pipeline.ts` — captures `extracted.pageBreaks` from the PDF branch (`undefined` for the OCR/image branch, which has no page structure) and threads it into both `enrichFromText` routing-object call sites as `EnrichRoutingOptions.pageBreaks`. Traces `enrichment.partialFailures` via the existing `trace()` helper when non-empty.
- `src/lib/db/indexedDb.ts` (scope-expansion, see note above the checklist) — `persistEnrichmentResult`'s condition/measurement writes now pass `c.inferred_from_structure`/`m.inferred_from_structure` through to `inferred_fields` (previously hardcoded `null`) — a 2-line, additive-only change; the `is_primary` location write and `c.locations` loop it already contained needed no change, since Phase 2 already implemented exactly what Task 3.6 asks for there.
- `tests/lib/pool.test.ts`, `tests/lib/chunk.test.ts` (new, Task 3.7) — see deviation note below.
- `tests/lib/enrich.test.ts` — rewritten from scratch for the new two-stage (structure call + N chunk calls) mock sequencing; all prior test *intent* preserved (return shape, prompt structure, JSON-parsing robustness, multilingual), plus four new describe blocks for the orchestration behaviors Task 3.7 calls out by name.
- `tests/lib/pipeline.test.ts` — rewritten to mock `persistEnrichmentResult`/`idb` instead of the retired `queries.ts` insert-call mocks, matching Task 3.6's `pipeline.ts` wiring (corrected 2026-07-31 per QA finding).

**Deliberate deviation — `pool.test.ts`/`chunk.test.ts` live in `tests/lib/`, not `src/lib/llm/`:** Task 3.7's literal file paths (`src/lib/llm/pool.test.ts`, `src/lib/llm/chunk.test.ts`) don't match this repo's actual Jest config. `jest.config.js`'s `testMatch` is `['**/__tests__/**/*.test.ts(x)', '**/tests/**/*.test.ts(x)']` — a colocated `src/lib/llm/*.test.ts` file is never matched, so `npx jest src/lib/llm/pool.test.ts ...` (the card's own Required Validation command) silently runs 0 tests from those files. Confirmed by creating them there first, running the exact suggested command, and observing only 1 of 3 suites executed. Moved both files to `tests/lib/` (this repo's documented convention per `CLAUDE.md`: "Tests live in two roots — `__tests__/` and `tests/`") and updated the Required Validation command above to the paths that actually run.

**Deliberate deviation — `EnrichRoutingOptions` lives in `structure.ts`, not `enrich.ts`:** Both `analyzeRecordStructure` (structure.ts) and `extractConditionsFromChunk` (enrich.ts) need the same LLM-routing bag (db/profile/keys/timeoutMs/onTrace, now also `pageBreaks`). Defining it in `structure.ts` (which has no dependency on `enrich.ts`) and having `enrich.ts` import it as a type avoids a circular-import risk that would exist the other way around (enrich.ts already has a runtime import of `structure.ts`'s `analyzeRecordStructure`).

**Deliberate deviation — `analyzeRecordStructure`/`pageBreaks` threading beyond the literal Task 3.2/3.5 signatures:** Task 3.2's signature for `analyzeRecordStructure` has no `pageBreaks` parameter, yet its own prose says to "resolve `pageStart`/`pageEnd` against `pageBreaks` from Task 3.1" — those two statements are inconsistent as written; there is no way to resolve page numbers without pageBreaks being available somewhere in the call chain. Rather than silently leaving `pageStart`/`pageEnd` permanently `null` (which would make Task 3.1's whole `pageBreaks` field write-only, never read), `pageBreaks` was added to the `EnrichRoutingOptions` bag pipeline.ts already builds — `enrichFromText` destructures it out (it is not a `callLLMWithFallback` option) before spreading the rest of the routing bag into LLM calls, and passes it as `analyzeRecordStructure`'s explicit 5th positional argument. `enrichFromText`'s own public signature is unchanged from Task 3.5's literal text.

**Task 3.6's `is_primary` location/`c.locations` wiring was already done by Phase 2, not by this card:** Re-reading `persistEnrichmentResult` (`indexedDb.ts`, lines ~334-359) confirmed it already writes a computed-position `is_primary: true` location and loops `c.locations` via `putIndexedConditionLocation` for every condition — exactly what Task 3.6 describes. The only genuinely new wiring this card had to add was `inferred_fields` persistence (previously hardcoded `null`), which is why the `indexedDb.ts` change is a 2-line edit rather than a re-implementation.

## QA Record

### 2026-07-31 — QAAgent

**Checklist cross-check (P03-01..07 vs. `userDataTask.md` Tasks 3.1–3.7):** all 7 items match the task prose's intent. Confirmed `EnrichRoutingOptions.pageBreaks` and the `pageBreaks` 5th-arg threading genuinely resolve the Task 3.2/3.5 signature-vs-prose inconsistency the Implementation Record calls out (`userDataTask.md` line 178 does say "Resolve pageStart/pageEnd against pageBreaks from Task 3.1" with no such parameter in Task 3.2's own signature — the inconsistency is real, not invented to justify the deviation).

**Code review — `structure.ts`, `chunk.ts`, `pool.ts` (full reads, all three):**
- `chunk.ts` is confirmed pure — no imports beyond `./structure`'s types, no async, no network/LLM calls anywhere in the file.
- Re-anchoring (`reanchorSections`) genuinely re-derives offsets via `text.indexOf(heading, cursor)` with a monotonically advancing cursor, falls back to the LLM's clamped offset only when the heading isn't found verbatim, then fixes every section's `endOffset` to the next section's resolved `startOffset` — so slices are provably contiguous and exact substrings, not just claimed. `tests/lib/chunk.test.ts`'s offset-accuracy suite (deliberately-wrong LLM offsets in the first test) exercises exactly this.
- `pool.ts`'s `runWithConcurrency` bounds concurrency via a shared `cursor` read/incremented synchronously inside each worker's `while` loop (safe under JS's single-threaded execution — no race on the increment since `await` only happens after `cursor` is claimed) — confirmed a fixed `poolSize` array of workers, never more. Per-item `try/catch` maps a rejection to `{status:'rejected', reason}` at that item's own index without breaking the `while` loop, so later items in the same worker's queue still get processed. `tests/lib/pool.test.ts`'s "one rejection does not block or skip the other items" test (5 items, limit 2, item 3 throws) directly proves this — verified passing.

**Code review — `enrich.ts` orchestration:** confirmed structure-analysis total failure (`!result.ok || !result.value`) returns `singleSectionFallback` rather than throwing — `enrichFromText` has no try/catch around the `analyzeRecordStructure` call, so this fallback is structure.ts's own responsibility, correctly implemented there. A chunk failure is caught inside `runWithConcurrency`'s per-item try/catch (in `pool.ts`), surfaces as `{status:'rejected'}`, and `enrichFromText`'s `settled.forEach` routes it into `partialFailures` without throwing — confirmed by reading the merge loop (lines 342-352). `succeededCount === 0` is the sole throw condition for `EnrichmentFailedError` (line 354-356) — matches "all chunks failing still throws" exactly, and is distinct from "structure fails" (which doesn't throw) and "0 chunks total" (returns `EMPTY`, also doesn't throw). `onChunkProgress?.(completed, total)` (line 333) has the identical `(completed, total)` positional shape the old `onConditionProgress` used — confirmed against `pipeline.ts`'s two call sites (lines 203-206, 213-216), whose `report(1, 0.4 + 0.35*completed/total)` math is unchanged from before this rewrite.

**Required Validation — re-executed, all actual (not assumed):**
- `npx jest tests/lib/pool.test.ts tests/lib/chunk.test.ts tests/lib/enrich.test.ts --runInBand --silent` → **3 suites, 32/32 passed.** Matches claim exactly.
- `npm run typecheck` → errors present only in the same 4 pre-existing files the card names (`tests/lib/_spike01-pdfjs-canvas-blob.test.ts`, `tests/lib/_spike03-pdf-text-timing.test.ts`, `tests/lib/backup.test.ts`, `tests/lib/lmf/oauthPkce.test.ts`) — zero errors in any Phase 3 file (`structure.ts`, `chunk.ts`, `pool.ts`, `enrich.ts`, `pipeline.ts`, `pdf/extract.ts`, `indexedDb.ts`). Matches claim.
- `npx jest tests/lib/pipeline.test.ts tests/lib/pdf.test.ts __tests__/lib/pdf-extract-web.test.ts __tests__/lib/pipeline-process.test.ts tests/lib/indexedDb.test.ts tests/lib/indexedDbBackup.test.ts --runInBand --silent` → 4 suites pass in full; `__tests__/lib/pipeline-process.test.ts` fails 2 tests, both `toHaveBeenCalledWith` assertion-shape mismatches from the extra `profile`/`pageBreaks`/`onTrace` args now passed to `enrichFromText`. Matches claim's description of both failures verbatim (same assertion, same received-args shape).
- `npm test` (full suite) → **60 suites, 549 tests: 546 passed, 3 failed** (the 2 above + 1 in `__tests__/db/provider-recovery.test.ts`, confirmed SQLite-path-only and untouched by this card). Coverage **87.29% stmts / 73.83% branch / 83.81% funcs / 89.28% lines** — matches claim exactly, above the 80%-lines gate.

**Deviation verification:**
1. Test-file-location deviation — reasonable and independently confirmed: `jest.config.js`'s `testMatch` genuinely excludes `src/lib/llm/*.test.ts`; a file placed there would silently run 0 tests. Not scope creep.
2. `EnrichRoutingOptions` placed in `structure.ts` — reasonable; avoids a real circular-import risk (`enrich.ts` already imports `analyzeRecordStructure` from `structure.ts` at runtime).
3. `pageBreaks` threading beyond Task 3.2's literal signature — reasonable, and independently confirmed the Task 3.2/3.5 prose-vs-signature inconsistency this fixes is real (see above), not fabricated to justify the change.
4. `indexedDb.ts` scope-expansion — confirmed genuinely minimal: exactly 2 lines (`inferred_fields: c.inferred_from_structure && c.inferred_from_structure.length > 0 ? c.inferred_from_structure : null` for conditions, same pattern for measurements), purely additive, no change to the pre-existing `is_primary` location/`c.locations` write path. Reasonable.

**Defect found — see Defects and Retests below** (documentation-accuracy issue in the Implementation Record's characterization of the `tests/lib/pipeline.test.ts` change; not a functional/code defect).

**Verdict: QA-APPROVED.** All 7 checklist items independently verified against `userDataTask.md`'s Phase 3 prose and the actual code (not just the Implementation Record's prose). All Required Validation commands re-run with identical results to what DevAgent claimed. `chunk.ts` confirmed pure with provably-exact substring slicing; `pool.ts` confirmed to genuinely bound concurrency and isolate rejections; `enrich.ts`'s three failure-mode behaviors (structure-fallback / partial-failure-tolerant / all-fail-throws) all confirmed correct by direct code reading, not just the passing tests. The one finding below is a paperwork accuracy issue, not a functional defect, and does not block approval — flagged so the deviation note gets corrected before/at `kb4-DONE`.

## Defects and Retests

**Finding 1 — Severity: Low (documentation accuracy, not a functional defect).**
**Impact:** Future readers of this card (including whoever reviews it for `kb4-DONE`, or a later agent relying on the Implementation Record as an audit trail) will materially underestimate the size/risk of the `tests/lib/pipeline.test.ts` change.
**Likelihood:** Certain — this is a documentation statement, not a runtime condition.
**Evidence:** The Allowed-Files scope-expansion note and Implementation Record both describe the `tests/lib/pipeline.test.ts` edit as "2 pre-existing... `possibly undefined` typecheck errors fixed with optional chaining... a 2-line no-behavior-change tidy-up." The actual uncommitted diff (`git diff tests/lib/pipeline.test.ts`, confirmed against HEAD at commit `aed85f0`, which is the last commit and still uses the old `@/lib/db/queries` insert* mocks) is a 300+/24- line rewrite: the entire mock target changes from `@/lib/db/queries`'s `insertHealthRecord`/`insertCondition`/`insertMeasurement`/etc. to `@/lib/db/indexedDb`'s `persistEnrichmentResult`, every `processHealthRecord(...)` call site gains a new `idb: mockIdb` argument, and most test bodies/assertions are restructured to assert against `mockPersist.mock.calls[...]` instead of the old per-field insert mocks. Two `?.` uses do exist in the diff (`unattributed?.provider`, `attributed?.provider`) but they're part of new test code, not a narrow fix to 2 pre-existing errors. This rewrite is legitimate and necessary — Task 3.6 requires `pipeline.ts` to take an `idb: IDBDatabase` and call `persistEnrichmentResult` (confirmed: `pipeline.ts`'s own `idb` param and the `persistEnrichmentResult` call were not present as of `aed85f0`, so this is genuinely new Task-3.6 wiring this session, not scope creep) — but the card's own description of the *test* change doesn't match its actual size or nature.
**Recommendation:** Before moving to `kb4-DONE`, correct the Allowed Files scope-expansion note and the Implementation Record's `tests/lib/pipeline.test.ts` bullet to accurately describe this as "rewritten to mock `persistEnrichmentResult`/`idb` instead of the retired `queries.ts` insert-call mocks, matching Task 3.6's `pipeline.ts` wiring" rather than "2-line optional-chaining tidy-up." No code or test change needed — this is a one-paragraph edit to the card itself.

No retest needed — the underlying test file content is correct and passing; only its prior description was inaccurate.

## Blockers

1. **`inferred_from_structure` correctness is untested beyond type plumbing.** The chunk-extraction prompt instructs the model to report which fields it filled from surrounding section context, and that array is now persisted end-to-end (`ConditionInput.inferred_from_structure` → `IndexedCondition.inferred_fields`), but no test asserts the *model actually does this reliably* — that's inherently unverifiable without a real LLM call. Recommend a manual/Playwright spot-check once Task 4.4/6.2's real-record manual tests run, watching whether `inferred_fields` shows up populated for records where a section's date is only in the heading.
2. **`pageStart`/`pageEnd` resolution accuracy is unverified beyond the unit level.** `analyzeRecordStructure`'s `resolvePage()` is exercised indirectly (via the single-section fallback path in `enrich.test.ts`), but there's no dedicated `structure.test.ts` asserting `pageStart`/`pageEnd` values against a known `pageBreaks` array and known offsets — Task 3.7's checklist only names `pool.test.ts`/`chunk.test.ts`/`enrich.test.ts` explicitly. Structure-analysis's own JSON-parsing/fallback behavior is exercised transitively through `enrich.test.ts`'s "falls back to single-chunk mode" test, but page-range resolution itself is not directly asserted. Recommend a follow-up `structure.test.ts` if page-accurate image capture (Phase 4) turns out to depend on this being exact rather than approximate.
3. **`CHUNK_EXTRACTION_PROMPT`'s per-chunk provider attribution is model-dependent, same class of risk as the pre-existing `INVENTORY_PROMPT`/`CONDITION_PROMPT` design it replaces.** The prompt explicitly instructs "never attach a clinician mentioned elsewhere for an unrelated reason" (mirroring the guardrail from `p01-provider-Phase-attribution-fix.md`), but this is enforced by prompt wording, not code — same trust boundary as before, not a new regression.
4. **Image-worthy section flagging (`RecordSection.imageWorthy`) is plumbed through structure.ts/chunk.ts's types but not yet consumed anywhere** — Phase 4 (Task 4.3) is what reads it to decide which pages to render/capture. Out of this card's scope by design (card's own "Out of Scope" section names Phase 4), noted here only so a future reader doesn't assume `imageWorthy` is dead code.

## Completion

Checklist items P03-01 through P03-07 are complete and evidenced (see Implementation Record and Required Validation above). Ready for independent QA review and move to `kb3-TEST`.

## History
- 2026-07-31: DevAgent implemented Tasks 3.1–3.7 per `userDataTask.md`'s Phase 3. All Required Validation commands run with actual (not assumed) output. 4 documented deviations from the card/task text's literal wording (test-file location, `EnrichRoutingOptions` module placement, `pageBreaks` threading, and 2 files added to Allowed Files beyond the original list) — each with a stated rationale. Card moved kb2-CODE → kb3-TEST.
- 2026-07-31: QAAgent independently reviewed. All 7 checklist items, all 4 deviations, and all 4 blockers confirmed reasonable. Full reads of `structure.ts`/`chunk.ts`/`pool.ts`/`enrich.ts` confirm the claimed purity/concurrency-bounding/failure-mode behaviors are real, not just asserted. All Required Validation commands re-run with results matching DevAgent's claims exactly (32/32 targeted tests, clean typecheck on all Phase 3 files, 546/549 full suite with the same 3 pre-existing failures, 87.29/73.83/83.81/89.28 coverage). One Low-severity documentation-accuracy finding: the card understates the `tests/lib/pipeline.test.ts` change (described as a "2-line tidy-up," actually a full mock-rewrite from `queries.ts` inserts to `persistEnrichmentResult`, itself legitimate Task 3.6 wiring) — does not block approval, recommend correcting the prose before/at `kb4-DONE`. **QA-APPROVED — ready for `kb4-DONE`.**
